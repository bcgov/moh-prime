@using Prime.Models
@model Document

@{
  var fileExt = Model.Filename.Split(".").Last();
  var base64 = Convert.ToBase64String(Model.Data);
  var imgSrc = String.Format($"data:image/{fileExt};base64,{0}", base64);
}

<img src="@imgSrc">
