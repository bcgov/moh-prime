import { TechnicalSupportsFormState } from './technical-supports-form-state';

describe('TechnicalSupportsFormState', () => {
  it('should create an instance', () => {
    expect(new TechnicalSupportsFormState()).toBeTruthy();
  });
});
