import { ConfigCodePipe } from './config-code.pipe';

describe('ConfigCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ConfigCodePipe();
    expect(pipe).toBeTruthy();
  });
});
