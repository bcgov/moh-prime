import { PageSubheaderTitleDirective } from './page-subheader-title.directive';

describe('PageSubheaderTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new PageSubheaderTitleDirective();
    expect(directive).toBeTruthy();
  });
});
