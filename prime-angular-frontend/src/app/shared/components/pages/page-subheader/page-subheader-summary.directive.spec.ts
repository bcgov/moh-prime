import { PageSubheaderSummaryDirective } from './page-subheader-summary.directive';

describe('PageSubheaderSummaryDirective', () => {
  it('should create an instance', () => {
    const directive = new PageSubheaderSummaryDirective();
    expect(directive).toBeTruthy();
  });
});
