import { FormControlValidityDirective } from './form-control-validity.directive';

describe('FormControlValidityDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlValidityDirective();
    expect(directive).toBeTruthy();
  });
});
