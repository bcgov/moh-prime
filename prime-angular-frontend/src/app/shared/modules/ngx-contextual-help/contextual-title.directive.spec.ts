import { ContextualTitleDirective } from './contextual-title.directive';

describe('ContextualTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextualTitleDirective();
    expect(directive).toBeTruthy();
  });
});
