import { UniquePecValidator } from './unique-pec-validator';

describe('UniquePecValidator', () => {
  it('should create an instance', () => {
    expect(new UniquePecValidator()).toBeTruthy();
  });
});
