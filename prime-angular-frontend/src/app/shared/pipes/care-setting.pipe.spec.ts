import { CareSettingPipe } from './care-setting.pipe';

describe('CareSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new CareSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
