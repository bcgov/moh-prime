import { PartyTypePipe } from './party-type.pipe';

describe('PartyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PartyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
