import { PageRefDirective } from './page-ref.directive';

describe('PageRefDirective', () => {
  it('should create an instance', () => {
    const directive = new PageRefDirective();
    expect(directive).toBeTruthy();
  });
});
