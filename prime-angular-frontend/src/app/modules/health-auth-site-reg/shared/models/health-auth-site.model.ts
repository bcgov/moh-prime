import { Site } from '@registration/shared/models/site.model';

// TODO what does this look like? Is it exactly the same?
// tslint:disable-next-line:no-empty-interface
export interface HealthAuthSite extends Site { }
