apiVersion: v1
kind: Service
metadata:
  name: {{ include "prime-docman.fullname" . }}
  labels:
    {{- include "prime-docman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prime-docman.selectorLabels" . | nindent 4 }}
