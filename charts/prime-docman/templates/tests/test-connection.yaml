apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prime-docman.fullname" . }}-test-connection"
  labels:
    {{- include "prime-docman.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prime-docman.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
