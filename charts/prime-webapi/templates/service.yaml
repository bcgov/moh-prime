apiVersion: v1
kind: Service
metadata:
  name: {{ include "prime-webapi.fullname" . }}
  labels:
    {{- include "prime-webapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prime-webapi.selectorLabels" . | nindent 4 }}